name: Test Workflow

on: [push]

permissions:
  contents: read

concurrency:
  group: test-workflow-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - run: echo "Test workflow loaded"
