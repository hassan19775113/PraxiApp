# Django / Core
DJANGO_SETTINGS_MODULE=praxi_backend.settings.prod
DJANGO_SECRET_KEY=CHANGE_ME
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,praxis-server

# Licensing
LICENSE_KEY=
CUSTOMER_ID=

# Database (PostgreSQL)
# Full connection string for the app database.
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/praxiapp_system

# E2E test credentials
# Used by CI agents and local auth validation scripts.
E2E_USER=e2e_ci
E2E_PASSWORD=e2e_ci_pw

# Auth / JWT
JWT_SIGNING_KEY=
JWT_ACCESS_MINUTES=30
JWT_REFRESH_DAYS=7

# CORS / CSRF
CORS_ALLOWED_ORIGINS=http://localhost:3000
CSRF_TRUSTED_ORIGINS=http://localhost

# Security (toggle for HTTP-only on-prem)
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_HSTS_SECONDS=0

# Email (optional)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=noreply@praxiapp.local

# Redis / Celery (optional)
REDIS_HOST=
REDIS_PORT=6379
CELERY_ENABLED=False

# Logging
DJANGO_LOG_DIR=/tmp
LOG_LEVEL=INFO
